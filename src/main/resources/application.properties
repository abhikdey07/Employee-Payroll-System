# Server
server.port=${PORT:8080}

# Database (Neon PostgreSQL)
# Database (Neon PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-ancient-firefly-ajp0jrzc-pooler.c-3.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

spring.datasource.username=neondb_owner

spring.datasource.password=npg_rOSt8cZh4mlD

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Mail (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=carelinkai.donation@gmail.com
spring.mail.password=wppfbnuxservpnrh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Session
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true

# Company Info
app.company.name=${COMPANY_NAME:CareLink AI}
app.company.address=${COMPANY_ADDRESS:SENHATI COLONY, BISHNUPUR BANKURA , India}
app.company.email=${COMPANY_EMAIL:carelinkai.donation@gmail.com}
app.company.phone=${COMPANY_PHONE:+91 9775339133}

# Default Admin
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD:Admin@123}

# Thymeleaf (only for email templates, not for static pages)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.check-template-location=true

# Logging
logging.level.com.payroll=INFO
logging.level.org.springframework.security=WARN
